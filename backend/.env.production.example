# ============================================================================
# Variables de Entorno - Ejemplo para Producción
# ============================================================================
# IMPORTANTE: Este es un archivo de ejemplo
# Copiar a .env y configurar con valores reales para producción
# NO COMMITEAR el archivo .env con valores reales a Git
# ============================================================================

# -----------------------------------------------------------------------------
# Base de Datos PostgreSQL
# -----------------------------------------------------------------------------
# URL de conexión completa
# Formato: postgresql+asyncpg://usuario:password@host:puerto/nombre_bd
DATABASE_URL=postgresql+asyncpg://hr_admin:CHANGE_ME_STRONG_PASSWORD@db:5432/hr_production

# -----------------------------------------------------------------------------
# Seguridad - JWT
# -----------------------------------------------------------------------------
# Generar SECRET_KEY con: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32

# Algoritmo de firma JWT (no cambiar a menos que sepas lo que haces)
ALGORITHM=HS256

# Tiempo de expiración de tokens
ACCESS_TOKEN_EXPIRE_MINUTES=120
REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# Entorno
# -----------------------------------------------------------------------------
# Valores: development, staging, production
ENV=production

# -----------------------------------------------------------------------------
# CORS - Configurar según tu frontend
# -----------------------------------------------------------------------------
# Orígenes permitidos separados por comas
# Ejemplos:
#   - Frontend único: https://app.tudominio.com
#   - Múltiples: https://app.tudominio.com,https://admin.tudominio.com
#   - Desarrollo local: http://localhost:3000,http://localhost:5173
CORS_ORIGINS=https://tu-frontend.com,https://app.tu-dominio.com

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Niveles: debug, info, warning, error, critical
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Aplicación
# -----------------------------------------------------------------------------
APP_NAME=Sistema de Gestión de RRHH
APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# NOTAS IMPORTANTES PARA PRODUCCIÓN
# -----------------------------------------------------------------------------
# 1. SECRET_KEY: DEBE ser único y generado aleatoriamente
#    Comando: openssl rand -hex 32
#
# 2. DATABASE_URL: Ajustar según tu configuración de PostgreSQL
#    - Cambiar host de 'db' a tu host real si no usas Docker
#    - Usar contraseña fuerte para el usuario de BD
#
# 3. CORS_ORIGINS: Solo incluir dominios autorizados
#    - Nunca usar '*' en producción
#    - Incluir protocolo (https://)
#    - Sin barra final
#
# 4. ENV: Debe ser 'production' en producción
#    - Afecta comportamiento de logging y debugging
#    - En 'production' se desactivan features de desarrollo
#
# 5. ACCESS_TOKEN_EXPIRE_MINUTES: Balance entre seguridad y UX
#    - Muy corto (5-10 min): Muy seguro pero molesto para usuarios
#    - Muy largo (>60 min): Conveniente pero menos seguro
#    - Actualmente: 120 minutos (2 horas)
